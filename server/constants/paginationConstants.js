/**
 * This module exports a constant used for pagination.
 * PAGINATION_LIMIT defines the maximum number of items that should be returned in a single page of results.
 */
module.exports = {
    PAGINATION_LIMIT: 10
}